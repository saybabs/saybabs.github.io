const pi = 3.14;

const add      = (x, y) => x + y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide   = (x, y) => x / y;

module.exports = {
    pi,
    add,
    subtract,
    multiply,
    divide
};